/**
 * Public brand names — keep in sync with Supabase defaults and Square catalog.
 */
export const SYNEXUS_BRAND_NAME = "SyNexus";

/** Subscription product name (Square catalog / checkout). */
export const SYNEXUS_PRO_PRODUCT_NAME = "SyNexusPro";

/** Default display name when signup has no username. */
export const SYNEXUS_MEMBER_DEFAULT_NAME = "SyNexus member";

/** Optional Square SKU for plan lookup (env SQUARE_PLAN_VARIATION_ID_PRO may use this). */
export const SYNEXUS_PRO_SKU = "SyNexusPro";
