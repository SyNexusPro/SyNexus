package com.synexus.app;

import static org.junit.Assert.*;

import org.junit.Test;

/** Local unit test — runs on the JVM, not a device. */
public class ExampleUnitTest {

    @Test
    public void addition_isCorrect() {
        assertEquals(4, 2 + 2);
    }
}
